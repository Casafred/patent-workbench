# Git冲突解决方案 - 已准备就绪 ✅

## 问题分析 🔍

你遇到的错误：
```
error: The following untracked working tree files would be overwritten by merge:
    gunicorn_config.py
Please move or remove them before you merge.
Aborting
```

**原因**：
- 服务器上有一个 `gunicorn_config.py` 文件（配置为 `0.0.0.0:5000`）
- 这个文件不在Git跟踪中
- GitHub上也有这个文件（配置为 `127.0.0.1:5000`）
- Git不知道该保留哪个，所以拒绝合并

---

## 解决方案 ✅

### 一键命令（复制粘贴）⭐

```bash
ssh root@43.99.101.195 "cd /home/appuser/patent-app && mv gunicorn_config.py gunicorn_config.py.server.backup && git pull origin main && chown -R appuser:appuser /home/appuser/patent-app && systemctl restart patent-app"
```

**这个命令会做什么？**
1. ✅ 备份服务器的配置文件（重命名为 `.server.backup`）
2. ✅ 拉取GitHub的新代码（包含功能八优化）
3. ✅ 修复文件权限
4. ✅ 重启服务

---

## 执行步骤 📋

### 1. 复制命令
打开 `一键解决冲突.txt`，复制第一个命令

### 2. 粘贴执行
在PowerShell中粘贴并回车

### 3. 输入密码
输入服务器密码

### 4. 等待完成
命令会自动执行所有步骤

### 5. 验证服务
```bash
# 检查服务状态
ssh root@43.99.101.195 "systemctl status patent-app | grep Active"

# 检查端口监听
ssh root@43.99.101.195 "netstat -tlnp | grep 5000"
```

### 6. 测试网站
浏览器访问：http://43.99.101.195

---

## 可能的情况 🔄

### 情况1：一切正常 ✅
- 服务状态：`Active: active (running)`
- 端口监听：`0.0.0.0:5000`
- 网站可访问
- **结果**：完成！可以测试功能八

### 情况2：服务无法访问 ⚠️
- 服务状态：正常
- 端口监听：`127.0.0.1:5000`（只监听本地）
- 网站无法访问
- **原因**：配置被GitHub的版本覆盖了
- **解决**：恢复服务器配置

```bash
ssh root@43.99.101.195 "cd /home/appuser/patent-app && cp gunicorn_config.py.server.backup gunicorn_config.py && systemctl restart patent-app"
```

---

## 配置说明 📝

### GitHub版本（本地开发用）
```python
bind = "127.0.0.1:5000"  # 只监听本地，安全
```

### 服务器版本（生产环境用）
```python
bind = "0.0.0.0:5000"  # 监听所有接口，可外部访问
```

**为什么不同？**
- 本地开发：只需要本机访问，用 `127.0.0.1` 更安全
- 服务器部署：需要外部访问，必须用 `0.0.0.0`

---

## 验证清单 ✅

完成后检查：

- [ ] 命令执行成功（无错误信息）
- [ ] 服务状态正常（Active: active (running)）
- [ ] 端口监听正确（0.0.0.0:5000）
- [ ] 网站可以访问（http://43.99.101.195）
- [ ] 功能八可以打开
- [ ] 新功能正常工作：
  - [ ] 图片高清显示
  - [ ] 鼠标滚轮缩放
  - [ ] +/- 按钮缩放
  - [ ] 弹窗查看功能
  - [ ] 标注框清晰

---

## 文档清单 📚

已创建的文档：
1. ✅ `阿里云回滚-准确命令.md` - 详细解决方案
2. ✅ `解决Git冲突.bat` - 自动化脚本
3. ✅ `一键解决冲突.txt` - 快速命令
4. ✅ `Git冲突解决完成.md` - 本文件

---

## 快速参考 🚀

### 解决冲突
```bash
ssh root@43.99.101.195 "cd /home/appuser/patent-app && mv gunicorn_config.py gunicorn_config.py.server.backup && git pull origin main && chown -R appuser:appuser /home/appuser/patent-app && systemctl restart patent-app"
```

### 恢复配置（如果需要）
```bash
ssh root@43.99.101.195 "cd /home/appuser/patent-app && cp gunicorn_config.py.server.backup gunicorn_config.py && systemctl restart patent-app"
```

### 验证状态
```bash
ssh root@43.99.101.195 "systemctl status patent-app | grep Active && netstat -tlnp | grep 5000"
```

---

## 下一步 ⏭️

1. **执行命令**：复制粘贴一键命令
2. **验证服务**：检查状态和端口
3. **测试网站**：访问并测试功能八
4. **清除缓存**：Ctrl+Shift+Delete
5. **体验新功能**：高清显示、缩放、弹窗

---

**创建时间**：2026-01-31  
**问题**：Git冲突 - gunicorn_config.py  
**状态**：✅ 解决方案已准备  
**下一步**：执行命令并验证
