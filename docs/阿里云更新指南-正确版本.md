# 阿里云服务器更新指南 - 正确版本 ✅

## 服务器实际配置

根据你提供的正确命令，服务器的实际配置是：

| 配置项 | 值 |
|--------|-----|
| 服务器IP | 43.99.101.195 |
| 项目路径 | /home/appuser/patent-app |
| 项目用户 | appuser |
| 服务名称 | patent-app |
| 端口 | 5000 |

## 🚀 快速更新

### 方法1：一键命令（最快）⭐

复制粘贴到本地CMD或PowerShell：

```bash
ssh root@43.99.101.195 "cd /home/appuser/patent-app && git pull origin main && chown -R appuser:appuser /home/appuser/patent-app && systemctl restart patent-app"
```

### 方法2：使用批处理脚本

双击运行：
```
正确的远程更新命令.bat
```

### 方法3：分步执行

```bash
# 1. 拉取代码
ssh root@43.99.101.195 "cd /home/appuser/patent-app && git pull origin main"

# 2. 修复权限
ssh root@43.99.101.195 "chown -R appuser:appuser /home/appuser/patent-app"

# 3. 重启服务
ssh root@43.99.101.195 "systemctl restart patent-app"

# 4. 检查状态
ssh root@43.99.101.195 "systemctl status patent-app"
```

## ✅ 验证命令

### 查看服务状态
```bash
ssh root@43.99.101.195 "systemctl status patent-app | grep Active"
```

### 查看端口监听
```bash
ssh root@43.99.101.195 "netstat -tlnp | grep 5000"
```

### 查看最新提交
```bash
ssh root@43.99.101.195 "cd /home/appuser/patent-app && git log --oneline -1"
```

### 查看实时日志
```bash
ssh root@43.99.101.195 "journalctl -u patent-app -f"
```

## 🔄 回滚命令

如果更新后出现问题：

```bash
ssh root@43.99.101.195 "cd /home/appuser/patent-app && git reset --hard HEAD^ && chown -R appuser:appuser /home/appuser/patent-app && systemctl restart patent-app"
```

## 📋 完整更新流程

### 1. 本地推送到GitHub
```bash
# 双击运行
快速推送功能八.bat
```

### 2. 远程更新服务器
```bash
# 双击运行
正确的远程更新命令.bat

# 或复制粘贴命令
ssh root@43.99.101.195 "cd /home/appuser/patent-app && git pull origin main && chown -R appuser:appuser /home/appuser/patent-app && systemctl restart patent-app"
```

### 3. 验证测试
1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 访问 http://43.99.101.195
3. 测试功能八新功能

## 🆘 常见问题

### Q1: git pull 失败？
```bash
# 查看状态
ssh root@43.99.101.195 "cd /home/appuser/patent-app && git status"

# 暂存本地修改
ssh root@43.99.101.195 "cd /home/appuser/patent-app && git stash && git pull && git stash pop"
```

### Q2: 服务重启失败？
```bash
# 查看日志
ssh root@43.99.101.195 "journalctl -u patent-app -n 50"

# 检查端口占用
ssh root@43.99.101.195 "lsof -i :5000"
```

### Q3: 权限问题？
```bash
# 修复权限
ssh root@43.99.101.195 "chown -R appuser:appuser /home/appuser/patent-app"

# 检查权限
ssh root@43.99.101.195 "ls -la /home/appuser/patent-app"
```

## 📊 配置对比

### 之前我误解的配置 ❌
- 路径：/www/wwwroot/patent-workbench
- 用户：root 或 www
- 服务：gunicorn

### 实际正确的配置 ✅
- 路径：/home/appuser/patent-app
- 用户：appuser
- 服务：patent-app

## 🎯 推荐使用

**最简单的方法：**

1. 双击 `正确的远程更新命令.bat`
2. 等待自动完成
3. 测试网站

或者直接复制粘贴这个命令：
```bash
ssh root@43.99.101.195 "cd /home/appuser/patent-app && git pull origin main && chown -R appuser:appuser /home/appuser/patent-app && systemctl restart patent-app"
```

---

**更新时间**：2026-01-31  
**服务器IP**：43.99.101.195  
**项目路径**：/home/appuser/patent-app  
**服务名称**：patent-app  
**状态**：✅ 已确认正确
