# 阿里云部署安全指南 - 2026-02-07

**重要性**: 🔴 HIGH  
**目的**: 确保当前代码安全推送到阿里云，并提供完整的回退方案

---

## 📊 当前代码状态评估

### ✅ 已完成的重构

1. **Chat功能模块化** (Task 5) ✅
   - 旧文件: `js/chat.js` (2243行)
   - 新模块: `js/modules/chat/` (7个模块)
   - **状态**: 已在 `frontend/index.html` 中使用新模块

2. **Claims Processor模块化** (Task 6) ✅
   - 旧文件: `js/claimsProcessorIntegrated.js` (3563行)
   - 新模块: `js/modules/claims/` (6个模块)
   - **状态**: 已在 `frontend/index.html` 中使用新模块

3. **组件化** (Task 2-3) ✅
   - HTML组件已提取到 `frontend/components/`
   - 使用 `component-loader.js` 动态加载
   - **状态**: 功能正常

### ⚠️ 潜在风险点

#### 风险1: 旧文件仍然存在
```
js/chat.js (2243行) - 旧版本
js/claimsProcessorIntegrated.js (3563行) - 旧版本
js/claimsProcessor.js (1329行) - 旧版本
```

**影响**: 
- 这些文件不会被加载（index.html已使用新模块）
- 但会占用服务器空间
- 可能造成维护混乱

**风险等级**: 🟡 MEDIUM（不影响功能，但建议清理）

#### 风险2: claims-processor.html 组件引用问题
```html
<!-- frontend/components/tabs/claims-processor.html -->
<script src="../js/chat.js?v=20260119"></script>
<script src="../js/asyncBatch.js?v=20260119"></script>
```

**影响**:
- 这个组件文件包含了完整的脚本引用
- 与主 index.html 重复
- 可能导致脚本重复加载

**风险等级**: 🟡 MEDIUM（需要修复但不紧急）

#### 风险3: Drawing Marker 版本混乱
```
6个版本的 drawingMarkerInteractive 文件同时存在
```

**影响**:
- 占用空间 ~140KB
- 不清楚哪个是当前版本
- 维护困难

**风险等级**: 🟡 MEDIUM（不影响功能）

---

## 🎯 部署风险评估

### 总体评估: 🟢 **可以安全部署**

#### 理由:
1. ✅ **核心功能已模块化**: Chat和Claims Processor已成功重构
2. ✅ **主入口文件正确**: `frontend/index.html` 使用新模块
3. ✅ **向后兼容**: 旧文件存在但不会被加载
4. ✅ **已测试**: 本地测试通过（根据文档记录）

#### 但需要注意:
- ⚠️ 旧文件会被推送到服务器（占用空间）
- ⚠️ 组件文件有重复引用（不影响功能）
- ⚠️ 版本文件混乱（需要后续清理）

---

## 🚀 推荐的部署策略

### 策略A: 直接部署（推荐）

**适用场景**: 你对当前代码有信心，想快速部署

**步骤**:
```bash
# 1. 提交当前代码
git add .
git commit -m "refactor: 完成Chat和Claims Processor模块化"
git push origin main

# 2. SSH登录阿里云
ssh root@你的服务器IP

# 3. 进入项目目录
cd /home/appuser/patent-app

# 4. 拉取最新代码
git pull origin main

# 5. 重启服务
systemctl restart patent-app

# 6. 验证
systemctl status patent-app
curl http://localhost:5001
```

**优点**:
- 快速
- 简单
- 风险低（旧文件不会被加载）

**缺点**:
- 旧文件会占用空间
- 需要后续清理

---

### 策略B: 清理后部署（最佳实践）

**适用场景**: 你想要一个干净的部署

**步骤**:

#### 第一步: 本地清理（可选）
```bash
# 备份旧文件
mkdir -p archive/old_versions_20260207
mv js/chat.js archive/old_versions_20260207/
mv js/claimsProcessorIntegrated.js archive/old_versions_20260207/
mv js/claimsProcessor.js archive/old_versions_20260207/
mv js/drawingMarkerInteractive_v*.js archive/old_versions_20260207/

# 提交清理
git add .
git commit -m "chore: 归档旧版本文件"
git push origin main
```

#### 第二步: 部署
```bash
# SSH登录阿里云
ssh root@你的服务器IP

# 进入项目目录
cd /home/appuser/patent-app

# 拉取最新代码
git pull origin main

# 重启服务
systemctl restart patent-app
```

**优点**:
- 代码库干净
- 易于维护
- 节省空间

**缺点**:
- 需要额外步骤
- 稍微复杂

---

## 🔄 完整的回退方案

### 方案1: Git回退（推荐）

#### 场景: 部署后发现问题，需要立即回退

```bash
# 1. SSH登录阿里云
ssh root@你的服务器IP

# 2. 进入项目目录
cd /home/appuser/patent-app

# 3. 查看当前commit
git log --oneline -5
# 输出示例:
# 6f1a40a (HEAD -> main) refactor: 完成模块化
# adcfb8a chore: 整理项目结构
# 23bada0 refactor: 提取API模块
# 906fa67 feat(功能八): 智能重新处理
# 988a968 fix(功能八): 优化重新处理

# 4. 回退到上一个稳定版本
# 假设 adcfb8a 是最后一个稳定版本
git reset --hard adcfb8a

# 5. 重启服务
systemctl restart patent-app

# 6. 验证
systemctl status patent-app
tail -f /home/appuser/patent-app/logs/error.log
```

**回退时间**: < 1分钟  
**数据丢失**: 无（只回退代码）

---

### 方案2: 创建回退点（最安全）

#### 部署前创建回退点

```bash
# 在阿里云服务器上执行

# 1. SSH登录
ssh root@你的服务器IP

# 2. 进入项目目录
cd /home/appuser/patent-app

# 3. 创建回退点分支
git branch rollback-point-20260207

# 4. 查看当前commit
git log -1
# 记录这个commit hash，例如: adcfb8a

# 5. 然后正常拉取新代码
git pull origin main

# 6. 重启服务
systemctl restart patent-app
```

#### 如果需要回退

```bash
# 1. SSH登录
ssh root@你的服务器IP

# 2. 进入项目目录
cd /home/appuser/patent-app

# 3. 切换到回退点
git checkout rollback-point-20260207

# 4. 重启服务
systemctl restart patent-app

# 5. 验证成功后，可以删除新代码
git branch -D main
git checkout -b main
git push -f origin main
```

**回退时间**: < 2分钟  
**数据丢失**: 无  
**可恢复性**: 100%

---

### 方案3: 完整备份（终极方案）

#### 部署前完整备份

```bash
# 在阿里云服务器上执行

# 1. SSH登录
ssh root@你的服务器IP

# 2. 停止服务
systemctl stop patent-app

# 3. 备份整个项目目录
cd /home/appuser
tar -czf patent-app-backup-20260207.tar.gz patent-app/

# 4. 备份数据库
sudo -u postgres pg_dump patent_db > patent_db-backup-20260207.sql

# 5. 验证备份
ls -lh patent-app-backup-20260207.tar.gz
ls -lh patent_db-backup-20260207.sql

# 6. 启动服务
systemctl start patent-app

# 7. 正常部署新代码
cd patent-app
git pull origin main
systemctl restart patent-app
```

#### 如果需要完整恢复

```bash
# 1. SSH登录
ssh root@你的服务器IP

# 2. 停止服务
systemctl stop patent-app

# 3. 删除当前项目
cd /home/appuser
rm -rf patent-app

# 4. 恢复备份
tar -xzf patent-app-backup-20260207.tar.gz

# 5. 恢复数据库
sudo -u postgres psql patent_db < patent_db-backup-20260207.sql

# 6. 启动服务
systemctl start patent-app

# 7. 验证
systemctl status patent-app
```

**回退时间**: 5-10分钟  
**数据丢失**: 无  
**可恢复性**: 100%

---

## 📋 部署前检查清单

### 本地检查

- [ ] 所有功能本地测试通过
- [ ] 无控制台错误
- [ ] 无语法错误
- [ ] Git状态干净（无未提交的更改）
- [ ] 已创建本地备份分支

### 服务器检查

- [ ] 服务器磁盘空间充足（至少1GB可用）
- [ ] 数据库正常运行
- [ ] 当前版本运行正常
- [ ] 已创建回退点或备份

### 部署后验证

- [ ] 服务启动成功
- [ ] 无错误日志
- [ ] 登录功能正常
- [ ] 核心功能测试通过
  - [ ] 功能一：即时对话
  - [ ] 功能二：异步批处理
  - [ ] 功能三：大批量处理
  - [ ] 功能四：本地专利库
  - [ ] 功能五：权利要求对比
  - [ ] 功能六：批量专利解读
  - [ ] 功能七：权利要求处理
  - [ ] 功能八：附图标记

---

## 🛡️ 安全部署流程（推荐）

### 第一步: 本地准备（5分钟）

```bash
# 1. 确保所有更改已提交
git status

# 2. 创建本地备份分支
git branch backup-before-deploy-20260207

# 3. 查看最近的提交
git log --oneline -5

# 4. 推送到GitHub
git push origin main
```

### 第二步: 服务器准备（3分钟）

```bash
# 1. SSH登录
ssh root@你的服务器IP

# 2. 进入项目目录
cd /home/appuser/patent-app

# 3. 查看当前状态
git status
git log -1

# 4. 创建回退点
git branch rollback-point-20260207

# 5. 记录当前commit hash
git log -1 --format="%H" > /tmp/last-stable-commit.txt
cat /tmp/last-stable-commit.txt
```

### 第三步: 部署（2分钟）

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 检查是否有冲突
git status

# 3. 重启服务
systemctl restart patent-app

# 4. 立即查看状态
systemctl status patent-app
```

### 第四步: 验证（5分钟）

```bash
# 1. 查看错误日志
tail -f /home/appuser/patent-app/logs/error.log
# 按 Ctrl+C 退出

# 2. 测试本地访问
curl http://localhost:5001

# 3. 在浏览器测试
# 访问: http://你的服务器IP

# 4. 测试核心功能
# - 登录
# - 上传文件
# - 专利查询
```

### 第五步: 回退（如果需要）

```bash
# 如果发现问题，立即回退

# 方法1: 快速回退到回退点
git reset --hard rollback-point-20260207
systemctl restart patent-app

# 方法2: 回退到记录的commit
LAST_COMMIT=$(cat /tmp/last-stable-commit.txt)
git reset --hard $LAST_COMMIT
systemctl restart patent-app

# 验证回退成功
systemctl status patent-app
git log -1
```

---

## 📞 故障排查

### 问题1: 服务无法启动

```bash
# 查看详细错误
journalctl -u patent-app -n 50

# 常见原因:
# 1. 语法错误 - 检查Python文件
# 2. 依赖缺失 - 重新安装: pip install -r requirements.txt
# 3. 端口占用 - 检查: netstat -tlnp | grep 5001
```

### 问题2: 404错误（找不到JS文件）

```bash
# 检查文件是否存在
ls -la /home/appuser/patent-app/js/modules/

# 检查Nginx配置
nginx -t
cat /etc/nginx/sites-available/patent-app

# 重启Nginx
systemctl restart nginx
```

### 问题3: 功能异常

```bash
# 查看浏览器控制台错误
# F12 -> Console

# 查看应用日志
tail -f /home/appuser/patent-app/logs/error.log

# 查看Nginx日志
tail -f /var/log/nginx/error.log
```

---

## 💡 最佳实践建议

### 1. 分阶段部署

```bash
# 第一阶段: 部署到测试环境
# 第二阶段: 小流量测试
# 第三阶段: 全量部署
```

### 2. 监控部署

```bash
# 实时监控日志
tail -f /home/appuser/patent-app/logs/error.log

# 监控系统资源
htop

# 监控网络连接
watch -n 1 'netstat -an | grep :5001 | wc -l'
```

### 3. 快速回退准备

```bash
# 创建一键回退脚本
cat > /home/appuser/quick-rollback.sh << 'EOF'
#!/bin/bash
cd /home/appuser/patent-app
git reset --hard $(cat /tmp/last-stable-commit.txt)
systemctl restart patent-app
systemctl status patent-app
EOF

chmod +x /home/appuser/quick-rollback.sh

# 使用: bash /home/appuser/quick-rollback.sh
```

---

## 📊 风险矩阵

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 服务无法启动 | 低 | 高 | 创建回退点 |
| 功能异常 | 中 | 中 | 充分测试 |
| 性能下降 | 低 | 低 | 监控资源 |
| 数据丢失 | 极低 | 高 | 备份数据库 |

---

## ✅ 结论

### 当前代码状态: 🟢 **可以安全部署**

**理由**:
1. 核心重构已完成且测试通过
2. 主入口文件使用新模块
3. 旧文件不会被加载
4. 有完整的回退方案

### 推荐部署方式:

**选择方案2: 创建回退点（最安全）**

**预计部署时间**: 10分钟  
**回退时间**: < 2分钟  
**风险等级**: 🟢 LOW

### 部署后建议:

1. **立即验证**: 测试所有核心功能
2. **监控日志**: 观察1小时，确保无异常
3. **清理旧文件**: 部署成功后，清理旧版本文件
4. **更新文档**: 记录部署过程和问题

---

## 📝 部署记录模板

```
部署日期: 2026-02-07
部署人员: [你的名字]
部署版本: 6f1a40a
回退点: adcfb8a

部署前检查:
- [✓] 本地测试通过
- [✓] 创建回退点
- [✓] 备份数据库

部署过程:
- [✓] 拉取代码
- [✓] 重启服务
- [✓] 验证功能

部署结果:
- 状态: 成功/失败
- 问题: 无/[描述问题]
- 回退: 否/是

备注:
[其他说明]
```

---

**文档版本**: 1.0  
**最后更新**: 2026-02-07  
**下次审查**: 部署完成后
