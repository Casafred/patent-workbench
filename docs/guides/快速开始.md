# 🚀 快速开始 - 3步部署到 Render

## 第一步：推送代码到 GitHub

### 最简单方法：双击运行脚本

```
快速推送到GitHub.bat
```

脚本会自动完成所有操作！

---

### 或者手动运行命令

在当前项目文件夹打开命令行，复制粘贴以下命令：

```cmd
git init
git remote add origin https://github.com/Casafred/patent-workbench.git
git add .
git commit -m "Deploy Patent Analysis Workbench"
git branch -M main
git push -u origin main -f
```

**如果提示需要登录：**
- 用户名：你的 GitHub 用户名
- 密码：你的 Personal Access Token（不是密码）

**获取 Personal Access Token：**
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 复制生成的 token

---

## 第二步：配置 Render 环境变量（可选）

1. 登录 Render Dashboard: https://dashboard.render.com/
2. 找到你的 Web Service
3. 进入 "Environment" 标签
4. 确认以下环境变量（通常会自动设置）：

```
FLASK_SECRET_KEY = (自动生成)
PORT = 10000
```

**注意：** 不需要在环境变量中配置 `ZHIPUAI_API_KEY`，用户登录后会在页面中自行配置自己的 API Key

---

## 第三步：等待部署完成

1. 在 Render Dashboard 查看 "Logs" 标签
2. 等待 3-5 分钟
3. 看到 "🚀 Application created successfully!" 表示成功
4. 访问你的 Render URL 测试

---

## ✅ 验证部署

访问你的应用（例如）：
```
https://patent-workbench.onrender.com
```

应该看到登录页面！

---

## 📞 遇到问题？

查看详细指南：
- `替换GitHub仓库代码步骤.md` - 详细的 Git 操作步骤
- `如何部署到Render.md` - 完整的部署指南
- `RENDER_DEPLOYMENT_GUIDE.md` - 问题排查

---

## 🎯 关键文件说明

- `wsgi.py` - 应用入口（Render 需要）
- `Procfile` - 启动命令
- `requirements.txt` - Python 依赖
- `render.yaml` - Render 配置

这些文件已经为你准备好了，不需要修改！

---

**就这么简单！** 🎉
