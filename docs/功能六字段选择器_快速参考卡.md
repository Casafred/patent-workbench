# 功能六字段选择器 - 快速参考卡

## 🎯 三大修复

```
┌─────────────────────────────────────────────────────────┐
│  1️⃣  法律事件显示修复                                    │
│  ✅ 问题：法律事件不显示                                 │
│  ✅ 原因：缺少 selected_fields 参数                      │
│  ✅ 修复：添加参数传递                                   │
│  ✅ 文件：js/main.js (第530-540行)                      │
├─────────────────────────────────────────────────────────┤
│  2️⃣  字段对应关系验证                                    │
│  ✅ 前端checkbox ID ↔ getSelectedFields()               │
│  ✅ getSelectedFields() ↔ 后端字段                      │
│  ✅ 后端字段 ↔ 显示逻辑                                  │
│  ✅ 结论：全部正确，无需修改                             │
├─────────────────────────────────────────────────────────┤
│  3️⃣  颜色改为淡绿色系                                    │
│  ✅ 蓝色系 → 绿色系                                      │
│  ✅ 主色：#43a047 (Green 600)                           │
│  ✅ 深色：#2e7d32 (Green 700)                           │
│  ✅ 淡色：#c8e6c9 (Green 100)                           │
│  ✅ 文件：frontend/css/components/field-selector.css    │
└─────────────────────────────────────────────────────────┘
```

## 📝 修改代码速览

### js/main.js (第530-540行)

```javascript
// ❌ 修改前
const results = await apiCall('/patent/search', {
    patent_numbers: uniquePatents,
    crawl_specification: crawlSpecification
});

// ✅ 修改后
const selectedFields = getSelectedFields();
const results = await apiCall('/patent/search', {
    patent_numbers: uniquePatents,
    crawl_specification: crawlSpecification,
    selected_fields: selectedFields  // ← 新增
});
```

### field-selector.css (颜色修改)

```css
/* ❌ 修改前（蓝色系） */
.field-selector-title svg { color: #4a6cf7; }
.field-option.checked { background: #e3f2fd; border-color: #4a6cf7; }
.field-option input[type="checkbox"] { accent-color: #4a6cf7; }

/* ✅ 修改后（绿色系） */
.field-selector-title svg { color: #43a047; }
.field-option.checked { background: #c8e6c9; border-color: #43a047; }
.field-option input[type="checkbox"] { accent-color: #43a047; }
```

## 🧪 快速测试（3分钟）

```
1️⃣  打开功能六 → 展开字段选择器
    ✅ 检查：颜色是绿色系（不是蓝色）

2️⃣  确认"事件时间轴"已勾选 → 输入专利号 → 查询
    ✅ 检查：控制台有 selected_fields 日志

3️⃣  点击专利条带 → 打开详情
    ✅ 检查：显示"事件时间轴"部分

4️⃣  取消勾选"事件时间轴" → 再次查询 → 打开详情
    ✅ 检查：不显示"事件时间轴"部分
```

## 📊 字段对应表（精简版）

| 前端 Checkbox ID | getSelectedFields() | 后端字段 | 状态 |
|-----------------|---------------------|---------|------|
| field_legal_events | legal_events | legal_events | ✅ |
| field_patent_citations | patent_citations | patent_citations | ✅ |
| field_cited_by | cited_by | cited_by | ✅ |
| field_classifications | classifications | classifications | ✅ |
| field_landscapes | landscapes | landscapes | ✅ |
| field_description | description | description | ✅ |
| field_drawings | drawings | drawings | ✅ |

**完整对应表**：见 `功能六字段选择器_完整修复总结_20260205.md`

## 🎨 颜色速查

```
主色调：#43a047  ████  Green 600 - 图标、按钮、复选框
深色：  #2e7d32  ████  Green 700 - 标题、文字
更深：  #1b5e20  ████  Green 800 - 选中文字
淡色：  #c8e6c9  ████  Green 100 - 边框、选中背景
极淡：  #e8f5e9  ████  Green 50  - 悬停背景
自定义：#f1f8f4  ████  Custom   - 选项背景
```

## 🚀 部署命令

```bash
# 1. 提交代码
git add js/main.js frontend/css/components/field-selector.css
git commit -F git_commit_field_selector_fix.txt
git push origin main

# 2. 部署（根据实际情况选择）
# 阿里云：
ssh user@server "cd /path/to/app && git pull && systemctl restart app"

# Render：
# 自动部署（推送后自动触发）

# 3. 验证
# 打开生产环境 → 测试法律事件显示 → 检查颜色
```

## 📁 文档清单

```
✅ 功能六字段选择器修复完成_20260205.md          (详细修复说明)
✅ 功能六字段选择器修复_快速测试.md              (测试指南)
✅ 功能六字段选择器_完整修复总结_20260205.md     (完整总结)
✅ 功能六字段选择器_颜色对比_20260205.md         (颜色对比)
✅ 功能六字段选择器_快速参考卡.md                (本文档)
✅ git_commit_field_selector_fix.txt             (Git提交信息)
```

## 🔍 问题排查

### 问题1：法律事件仍不显示
```
检查：控制台是否有 selected_fields 日志？
  ├─ 有 → 检查后端是否收到参数
  └─ 无 → 清除缓存，强制刷新（Ctrl+Shift+R）
```

### 问题2：颜色不对
```
检查：CSS文件是否正确加载？
  ├─ 是 → 检查是否有其他CSS覆盖
  └─ 否 → 清除缓存，检查文件路径
```

### 问题3：字段选择不生效
```
检查：checkbox的ID是否正确？
  ├─ 是 → 检查 getSelectedFields() 函数
  └─ 否 → 修正checkbox ID
```

## 📞 联系方式

```
问题反馈：
- GitHub Issues
- 项目文档
- 开发团队

紧急问题：
- 查看控制台错误
- 检查网络请求
- 回滚到上一版本
```

## ✅ 完成检查清单

```
□ 代码已修改
□ 本地测试通过
□ 控制台无错误
□ 法律事件正确显示
□ 颜色为淡绿色系
□ 字段选择功能正常
□ 文档已创建
□ Git已提交
□ 已部署到生产
□ 生产环境验证通过
```

---

**快速参考卡版本**：v1.0
**创建时间**：2026-02-05
**适用范围**：功能六字段选择器修复
**打印建议**：A4纸，双面打印
