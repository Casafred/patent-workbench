## 修复计划

### 问题1：标签页切换按钮样式修复
**文件**: `frontend/css/components/tabs.css`

添加 `.mode-switcher-tab` 类的样式定义：
- 非激活状态：深色背景 + 浅色文字
- 激活状态：主题色背景 + 白色文字
- 悬停效果：增强视觉反馈

### 问题2：智能分类标引界面初始化修复
**文件**: `js/modules/init/init-unified-batch.js`

修改 `switchUnifiedMode` 函数：
- 当切换到分类模式时，确保调用 `ClassificationModule.initUI()` 来渲染界面
- 移除 `_initialized` 检查的限制，确保每次切换都能正确渲染