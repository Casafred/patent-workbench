## 修复计划

### 修复一：重构模式切换按钮样式
**文件**: `frontend/components/tabs/unified-batch.html`

移除内联样式，改用 CSS 类来控制样式：
- 为按钮添加更明显的背景色和边框
- 提高非激活状态下的字体对比度
- 添加 hover 效果增强可点击感

**修改内容**:
1. 移除按钮的内联 style 属性
2. 添加 `mode-switcher-tab` 类的样式定义到 `tabs.css`

### 修复二：修复智能标引页初始加载问题
**文件**: `js/modules/init/init-unified-batch.js`

修改 `switchUnifiedMode` 函数，确保：
1. 在面板显示后再调用 `initUI()`
2. 确保步骤一的 stepper-item 和 sub-tab-content 正确激活

**修改内容**:
1. 调整初始化顺序，先显示面板再初始化 UI
2. 添加强制刷新步骤一 UI 的逻辑