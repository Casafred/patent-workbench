## 修复计划

### 1. 修复搜索按钮位置问题
**文件**: `frontend/css/pages/prompt-forum.css`

修改 `.forum-search-btn` 样式：
- 添加明确的 `width` 和 `height`（如 32px）
- 添加 `padding: 0` 避免内边距撑大按钮
- 确保按钮在输入框右侧正确居中

### 2. 修复提示词导入功能
**文件**: `js/modules/prompt-forum/prompt-forum.js`

修改 `importToFeature1` 函数（即时聊天导入）：
- 直接操作 `appState.chat.personas` 添加新角色
- 使用 `window.userCacheStorage` 保存数据
- 调用 `updatePersonaSelector()` 和 `updatePersonaEditor()` 更新UI
- 暴露必要的函数到全局或使用已暴露的函数

修改 `importToFeature5` 函数（批量处理导入）：
- 使用 `window.UnifiedBatch.template.importTemplate()` 方法
- 正确处理导入结果和错误

修改 `importPrompt` 函数：
- 移除对不存在的 `window.switchToTab` 的调用
- 使用正确的页面切换方式

### 3. 检查并暴露必要的全局函数
确保以下函数可被论坛模块调用：
- `loadPersonas` / `savePersonas` / `updatePersonaSelector` / `updatePersonaEditor`
- 或者在论坛模块中直接操作状态和存储