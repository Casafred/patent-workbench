## 优化方案

### 方案A: 修复非分阶段API（推荐，改动最小）

**修改文件**: `backend/routes/drawing_marker.py`

在AI模式处理前，添加说明书预处理：

```python
# 在 Step 2 之前添加预处理
if ai_mode:
    # 🔧 修复：先提取相关段落，再发给AI
    from backend.utils.text_segment_extractor import extract_relevant_segments
    
    extraction_result = extract_relevant_segments(
        specification, 
        all_ocr_markers,  # 使用OCR识别的标记
        context_sentences=1,
        max_total_length=8000
    )
    specification_to_process = extraction_result['extracted_text']
    print(f"[DEBUG] 说明书预处理: {len(specification)} -> {len(specification_to_process)} 字符")
else:
    specification_to_process = specification
```

### 方案B: 修复分阶段API的数据传递

**修改文件**: 
1. `frontend/index.html` - AI阶段请求携带extracted_text
2. `backend/routes/drawing_marker.py` - 接收并使用前端传来的extracted_text

### 方案C: 统一处理逻辑（最彻底）

将非分阶段API改为调用分阶段逻辑，确保所有路径都经过预处理。

---

**建议先实施方案A**，因为：
1. 改动最小，只需修改一个文件
2. 保持API兼容性
3. 直接解决超时问题

是否确认执行此优化方案？