## 计划：智能分割 + PP-OCRv5集成

### 第一阶段：创建智能分割工具

**步骤1.1** 创建 `backend/utils/smart_split_utils.py`
- 实现基于说明书标记长度的智能分割函数
- `find_split_pattern()` - 找到合适的分割模式
- `smart_split_ocr_results()` - 主分割函数
- `split_text_by_pattern()` - 按模式分割文本

### 第二阶段：更新OCR工具模块

**步骤2.1** 更新 `glm_ocr_utils.py`
- 添加智能分割调用
- 在返回结果前应用分割

**步骤2.2** 创建 `paddle_ocr_utils.py`
- 实现PP-OCRv5 API调用
- 解析 `dt_polys`、`rec_texts`、`rec_scores`
- 应用智能分割

### 第三阶段：后端集成

**步骤3.1** 修改 `drawing_marker.py`
- 添加 `ocr_mode: 'rapidocr' | 'glm_ocr' | 'paddle_ocr'`
- 在OCR结果处理流程中集成智能分割
- 传递说明书标记列表用于分割参考

### 第四阶段：前端更新

**步骤4.1** 更新 `drawing-marker.html`
- 下拉框添加 `PP-OCRv5 (百度)` 选项

**步骤4.2** 更新前端JS
- 传递 `ocr_mode` 参数

### 第五阶段：测试验证

**步骤5.1** 运行测试脚本验证
- 测试PP-OCRv5识别效果
- 验证智能分割功能