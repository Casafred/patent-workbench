## 实现计划

### 任务一：在AI对比分析前显示同族各权利要求原文

**修改文件**: `js/familyClaimsComparison.js`

**实现方案**:
1. 修改 `compareFamilyClaims()` 函数，分两步执行：
   - **第一步**：先爬取各专利的权利要求原文，显示在结果区域
   - **第二步**：用户点击"开始AI对比分析"按钮后，再调用AI进行对比

2. 新增函数：
   - `fetchFamilyClaimsOriginal()`: 获取并显示各专利的权利要求原文
   - `renderOriginalClaimsPreview()`: 渲染原文预览界面（并排显示各专利的权利要求原文）
   
3. 修改按钮逻辑：
   - 点击"开始对比分析"按钮后，先显示原文预览
   - 原文预览区域添加"开始AI对比分析"按钮
   - 用户确认后再进行AI分析

### 任务二：修改"获取同族列表"按钮样式

**修改文件**: `frontend/css/pages/family-claims-comparison.css`

**实现方案**:
1. 调整 `#fetch_family_btn` 按钮样式：
   - 减小padding使按钮更紧凑
   - 确保文字横向排列
   - 调整按钮高度

2. 可能需要调整 `.config-row` 的flex布局，确保按钮正常显示