## 修复计划

### 问题1：双栏模式下右侧栏无法展开

**修改文件**: `js/patentDetailNewTab.js`

**修改内容**:
1. 修改 `toggleSection` 函数，让它接收事件对象或元素引用，找到当前点击的具体section
2. 将 `onclick="toggleSection('sectionId')"` 改为 `onclick="toggleSection(event, 'sectionId')"` 
3. 在函数内部使用 `event.currentTarget.closest('.collapsible-section')` 找到当前操作的section

### 问题2：相关专利区域的按钮右对齐问题

**修改文件**: `js/patentDetailNewTab.js`

**修改内容**:
1. 检查并确保 `.section-actions` 容器正确应用 `justify-content: flex-end` 
2. 为按钮容器添加 `width: 100%` 确保占满整行
3. 检查是否有父容器样式影响，必要时添加明确的右对齐样式

---

这两个修改都在同一个文件中进行，修改量较小，可以一起完成。