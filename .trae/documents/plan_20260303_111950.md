## 修改计划

### 1. 修复 base64Data 类型错误
**文件**: `js/modules/drawing-marker/drawing-marker-init.js`

在处理 base64 数据前添加类型检查：
- 检查 `proxyResult.data` 是否为字符串
- 如果是对象，尝试提取实际的 base64 字符串
- 添加详细的错误日志

### 2. 修复按钮样式
**文件**: `js/main.js`

为所有按钮添加明确的文字颜色：
- "取消"按钮添加 `color: #666`
- 确保所有按钮样式在深色主题下也可见