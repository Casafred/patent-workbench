## 修复计划

### 1. 修复暗夜模式下对话参数弹窗顶部白色问题
**文件**: `frontend/css/base/dark-theme.css`
- 添加 `#chat_params_modal .modal-header` 的暗夜模式样式，设置背景色为暗色

### 2. 修复弹窗先出现在右下角再闪到中间的问题
**文件**: `frontend/css/components/modals.css`
- 修改 `.modal-content` 的初始样式，确保初始状态就居中定位
- 添加 `.modal` 初始状态样式，避免闪烁

### 3. 修复消息管理状态下对话气泡同一侧对齐
**文件**: `frontend/css/pages/chat.css`
- 修改 `.chat-window-management-mode .user-message .message-main-content` 样式
- 让用户消息的气泡内容也左对齐（取消 row-reverse）
- 确保勾选框和消息内容都在同一侧