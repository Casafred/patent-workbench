## 通用智能剪贴板系统 (Universal Smart Clipboard)

基于对各功能输入输出端口的全面盘点，设计一个一劳永逸的通用粘贴板系统。

---

### 核心设计理念

```
┌─────────────────────────────────────────────────────────────────┐
│                    通用智能剪贴板系统                             │
│                                                                  │
│   用户复制内容 ──▶ 自动类型识别 ──▶ 智能匹配目标 ──▶ 一键粘贴    │
│                       ↓                                          │
│              ┌─────────────────┐                                 │
│              │  内容类型注册表  │                                 │
│              │  - 专利号列表    │                                 │
│              │  - 权利要求文本  │                                 │
│              │  - 专利表格      │                                 │
│              │  - AI分析结果    │                                 │
│              │  - 普通文本      │                                 │
│              └─────────────────┘                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### 各功能输入输出盘点总结

| 功能 | 主要输入 | 主要输出 | 数据承接潜力 |
|-----|---------|---------|-------------|
| **功能一** | 对话输入、文件 | AI回复、搜索结果 | ⭐⭐⭐ 高 - AI回复可发送到多处 |
| **功能二** | Excel、手动输入 | 结果表格 | ⭐⭐ 中 - 结果可导出给功能三/四 |
| **功能三** | Excel、JSONL | 批处理报告 | ⭐⭐ 中 - 报告可给功能四 |
| **功能四** | Excel(原始库/新库) | 展开专利号、合并表 | ⭐⭐⭐ 高 - 专利号给功能六 |
| **功能五** | 多版本权利要求 | 对比报告 | ⭐⭐ 中 - 报告可讨论 |
| **功能六** | 专利号列表 | 专利详情、解读 | ⭐⭐⭐ 高 - 权利要求给五/七，附图给八 |
| **功能七** | Excel、权利要求文本 | 分析表格、可视化 | ⭐⭐ 中 - 结果给功能五 |
| **功能八** | 图片、说明书 | 标注附图 | ⭐ 低 - 截图输出 |

---

### 内容类型自动识别规则

```javascript
const ContentTypeRules = {
  // 专利号列表：CN/US开头，数字+字母组合，多行或逗号分隔
  'patent-numbers': {
    patterns: [
      /^(CN|US|EP|WO|JP|KR)[\d\.]+[A-Z\d]?$/mi,  // 单行匹配
      /(?:CN|US|EP|WO|JP|KR)[\d\.]+[A-Z\d]?/g     // 提取模式
    ],
    minMatches: 1,
    priority: 10
  },
  
  // 权利要求文本：包含"权利要求"、"其特征在于"等关键词
  'claims-text': {
    patterns: [
      /权利要求[书\s]*[\d一二三四五六七八九十]+/,
      /其特征在于/,
      /^(1|一)[\.、\s]+.*?(?:其特征在于|characterized)/m
    ],
    minMatches: 2,
    priority: 9
  },
  
  // 专利表格：包含表头行，多列数据
  'patent-table': {
    patterns: [
      /公开号|专利号|申请号.*标题|发明名称/i,  // 表头检测
      /\t.*\t.*\t/,                           // 制表符分隔
      /[,;]\s*CN\d+/                          // CSV格式含专利号
    ],
    minMatches: 1,
    priority: 8
  },
  
  // AI分析结果：Markdown格式、结构化输出
  'ai-analysis': {
    patterns: [
      /^#{1,6}\s+/m,           // Markdown标题
      /```[\s\S]*?```/,        // 代码块
      /\*\*.*?\*\*/,           // 加粗
      /\[.*?\]\(.*?\)/         // 链接
    ],
    minMatches: 2,
    priority: 5
  },
  
  // 普通文本
  'plain-text': {
    patterns: [/.+/],
    minMatches: 1,
    priority: 1
  }
};
```

---

### 目标位置匹配表

```javascript
const TargetMappings = {
  // 专利号列表 可粘贴到
  'patent-numbers': [
    { target: '#patent_numbers_input', label: '功能六-专利号输入', action: 'replace' },
    { target: '#lpl_family_col_name', label: '功能四-同族列名', action: 'info' },
    { target: '#async_manual_input', label: '功能二-手动输入', action: 'replace' }
  ],
  
  // 权利要求文本 可粘贴到
  'claims-text': [
    { target: '#claims_text_input', label: '功能七-文本分析', action: 'replace' },
    { target: '[id^="claim_text_"]', label: '功能五-对比输入', action: 'append' },
    { target: '#chat_input', label: '功能一-对话', action: 'append' }
  ],
  
  // 专利表格 可粘贴到
  'patent-table': [
    { target: '#lpl_new_file_input', label: '功能四-新库文件', action: 'file-simulate' },
    { target: '#gen_file-input', label: '功能三-Excel上传', action: 'file-simulate' },
    { target: '#claims_excel_file', label: '功能七-Excel分析', action: 'file-simulate' }
  ],
  
  // AI分析结果 可粘贴到
  'ai-analysis': [
    { target: '#chat_input', label: '功能一-讨论', action: 'append' },
    { target: '#claims_text_input', label: '功能七-分析', action: 'replace' },
    { target: '#async_system_prompt', label: '功能二-系统提示', action: 'replace' }
  ],
  
  // 普通文本 通用
  'plain-text': [
    { target: 'textarea:not([readonly])', label: '任意文本框', action: 'focus-paste' }
  ]
};
```

---

### 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        SmartClipboard 核心                      │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ 内容检测器   │  │ 类型识别器   │  │ 目标推荐引擎             │  │
│  │ - 监听复制   │  │ - 规则匹配   │  │ - 匹配可用目标           │  │
│  │ - 监听选择   │  │ - 置信度计算 │  │ - 排序推荐               │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ 浮动面板UI  │  │ 快捷粘贴按钮 │  │ 历史记录管理             │  │
│  │ - 预览内容   │  │ - 输入框旁   │  │ - 最近10条              │  │
│  │ - 目标列表   │  │ - 智能提示   │  │ - 持久化存储            │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

### UI 设计

#### 1. 全局浮动剪贴板面板
```
┌─────────────────────────────────────────────────────────┐
│  📋 智能剪贴板                              [×] [−]    │
├─────────────────────────────────────────────────────────┤
│  检测到: 专利号列表 (置信度: 95%)                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ CN201810123456.7                                │   │
│  │ US10,123,456 B2                                 │   │
│  │ EP1234567A1                                     │   │
│  │ ... 共12个                                      │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  推荐目标:                                               │
│  [发送到 功能六-专利号输入]  [发送到 功能二-手动输入]     │
│  [发送到 功能四-同族展开]                               │
├─────────────────────────────────────────────────────────┤
│  历史 ▼                                                  │
│  ○ 权利要求文本 (功能一) - 3分钟前                      │
│  ○ AI分析结果 (功能六) - 10分钟前                       │
└─────────────────────────────────────────────────────────┘
```

#### 2. 输入框智能粘贴按钮
```html
<!-- 自动添加到所有相关输入框旁 -->
<div class="input-wrapper">
  <textarea id="patent_numbers_input"></textarea>
  <button class="smart-paste-btn" title="智能粘贴">
    📋
    <span class="paste-badge" data-type="patent-numbers">3</span>
  </button>
  <!-- 点击后显示下拉菜单 -->
  <div class="smart-paste-menu">
    <div class="paste-item" data-content="CN2018...">专利号列表 (12个)</div>
    <div class="paste-item" data-content="权利要求...">权利要求文本</div>
  </div>
</div>
```

---

### 核心代码结构

```javascript
// js/core/smart-clipboard.js
class SmartClipboard {
  constructor() {
    this.history = [];
    this.current = null;
    this.maxHistory = 10;
    this.init();
  }
  
  init() {
    // 监听复制事件
    document.addEventListener('copy', (e) => this.handleCopy(e));
    // 监听自定义导出事件
    document.addEventListener('content-export', (e) => this.handleExport(e));
    // 创建UI
    this.createPanel();
    this.injectPasteButtons();
  }
  
  // 自动识别内容类型
  detectType(text) {
    for (const [type, config] of Object.entries(ContentTypeRules)) {
      let matchCount = 0;
      for (const pattern of config.patterns) {
        if (pattern.test(text)) matchCount++;
      }
      if (matchCount >= config.minMatches) {
        return { type, priority: config.priority, confidence: matchCount / config.patterns.length };
      }
    }
    return { type: 'plain-text', priority: 1, confidence: 1 };
  }
  
  // 获取可用的粘贴目标
  getValidTargets(contentType) {
    const targets = TargetMappings[contentType] || [];
    return targets.filter(t => {
      const el = document.querySelector(t.target);
      return el && this.isElementVisible(el);
    });
  }
  
  // 粘贴到目标
  pasteTo(target, content, action = 'replace') {
    const el = document.querySelector(target);
    if (!el) return;
    
    switch(action) {
      case 'replace':
        el.value = content;
        break;
      case 'append':
        el.value += '\n' + content;
        break;
      case 'focus-paste':
        el.focus();
        document.execCommand('insertText', false, content);
        break;
    }
    
    // 触发事件
    el.dispatchEvent(new Event('input', { bubbles: true }));
    el.dispatchEvent(new Event('change', { bubbles: true }));
  }
}

// 全局实例
window.smartClipboard = new SmartClipboard();
```

---

### 功能模块集成点

| 功能 | 集成方式 | 导出触发点 |
|-----|---------|-----------|
| 功能一 | AI回复旁添加"复制到剪贴板"按钮 | 每条回复 |
| 功能二 | 结果表格行添加右键菜单"发送" | 选中行 |
| 功能三 | 报告预览区添加"复制数据" | 完整报告 |
| 功能四 | 展开结果旁添加"发送到功能六" | 专利号列表 |
| 功能五 | 对比结果添加"复制权利要求" | 各版本文本 |
| 功能六 | 专利详情弹窗添加"发送到..." | 权利要求、附图 |
| 功能七 | 分析表格行添加"复制" | 单行/多选 |
| 功能八 | 标注结果添加"复制截图" | 完成标注后 |

---

### 实施文件清单

**新增文件:**
1. `js/core/smart-clipboard.js` - 核心逻辑
2. `js/core/content-types.js` - 类型识别规则
3. `frontend/css/smart-clipboard.css` - 样式
4. `frontend/components/smart-clipboard-panel.html` - 面板模板

**修改文件:**
1. `js/main.js` - 初始化SmartClipboard
2. `js/modules/chat/chat-core.js` - 添加导出按钮
3. `js/modules/claims/claims-core.js` - 添加导出功能
4. `js/localPatentLib.js` - 添加发送到功能六
5. `js/patentChat.js` - 添加发送到功能五/七/八
6. `frontend/index.html` - 引入样式和脚本

---

### 优先级建议

1. **P0** - 核心剪贴板 + 专利号列表识别 (功能四→六)
2. **P1** - 权利要求文本识别 (功能六→五/七)
3. **P2** - AI回复导出 (功能一→任意)
4. **P3** - 表格数据传递 (功能二/三→四)

请确认此方案后，我将开始具体实现。