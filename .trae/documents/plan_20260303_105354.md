## 修改计划

### 修改文件: `js/patentDetailNewTab.js`

### 1. 翻译弹窗可同时显示多个
- 移除 `showTranslationResultNewTab` 函数中的 `if (existingResult) existingResult.remove();` 
- 为每个翻译弹窗生成唯一ID（基于textType和timestamp）
- 允许权利要求和说明书的翻译弹窗同时存在

### 2. 翻译弹窗可拖拽和调节大小
- 添加拖拽功能：通过标题栏拖拽移动弹窗位置
- 添加调整大小功能：在右下角添加拖拽调整手柄
- 添加相关CSS样式和JS事件处理

### 3. 双栏模式下点击侧翻译后替换该侧原文
- 修改翻译按钮的onclick，传递事件对象和侧栏信息（left/right）
- 在 `startTranslationNewTab` 函数中记录点击的侧栏
- 翻译完成后调用新函数 `replaceContentInColumn` 替换对应侧栏的原文
- 权利要求：分条替换，保持原有的编号和样式
- 说明书：整体替换内容

### 具体修改内容：
1. 修改翻译按钮HTML，添加侧栏标识参数
2. 重写 `showTranslationResultNewTab` 函数，支持多弹窗和拖拽/调整大小
3. 新增 `replaceContentInColumn` 函数，实现内容替换
4. 添加拖拽和调整大小的事件处理代码