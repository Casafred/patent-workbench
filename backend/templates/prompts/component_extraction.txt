你是一个专利说明书分析专家。请从以下专利说明书中抽取附图标记及其对应的部件名称。

**重要说明:**
以下内容已经过预处理，每行都是包含特定附图标记的完整句子，格式为"[标记XX] 句子内容"。
请根据这些句子，提取每个标记对应的部件名称。

**抽取规则:**
1. 附图标记通常是数字(如10、20、100)或字母数字组合(如A1、B2)
2. 部件名称通常出现在标记之前，如"外壳10"、"显示屏20"
3. 如果一个标记有多个可能的名称，选择最准确、最完整的一个
4. 如果无法确定部件名称，可以将标记本身作为名称
5. 忽略纯图号引用(如"如图1所示")

**输出格式要求:**
必须严格按照以下JSON格式输出，不要包含任何其他文字:

```json
{
  "components": [
    {"marker": "10", "name": "外壳"},
    {"marker": "20", "name": "显示屏"}
  ]
}
```

**待处理的说明书内容:**
{description_text}
