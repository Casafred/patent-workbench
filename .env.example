# 环境变量示例文件
# 复制此文件为 .env 并填入实际值（仅用于本地开发）
# 在 Render 上，这些变量需要在 Dashboard 中配置

# Flask 配置
FLASK_SECRET_KEY=your-secret-key-here-change-in-production

# 数据库配置（可选，如果不设置则使用文件存储）
# DATABASE_URL=postgresql://user:password@host:port/database

# IP限制配置
MAX_IPS_PER_USER=5

# 服务器配置
PORT=5001

# 注意：不需要在环境变量中配置 ZHIPUAI_API_KEY
# 用户登录后在页面中自行配置自己的 API Key

